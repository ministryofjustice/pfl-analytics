apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: pfl-analytics-dev
spec:
  selector:
    app: pfl-analytics-dev
  ports:
    - port: 8501
      targetPort: 8501
      name: http
